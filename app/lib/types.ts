export type IsMobileProps = {
  $isMobile: boolean;
};

export type RatesApiDates = {
  start_date: string;
  end_date: string;
};

export type FormContent = {
  email: string;
  phone: string;
  name: string;
  message: string;
};
